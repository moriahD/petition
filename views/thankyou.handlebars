{{> nav }}

<p>Thank you, {{first_name}} for your support.</p>
<a href="{{url}}">See all {{number}}signers</a>
<img src="{{imgUrl}}" alt="users signature">

<form method="POST">
    <button type="button" name="delete_btn" id="delete_btn">{{delete}}</button>
</form>
